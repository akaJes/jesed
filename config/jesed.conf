<Location /jesed/>
    ProxyPass http://localhost:9353/
    ProxyPassReverse /

    RewriteEngine on
    RewriteCond %{HTTP:UPGRADE} ^WebSocket$ [NC]
    RewriteCond %{HTTP:CONNECTION} Upgrade$ [NC]
    RewriteRule /(\w*)/ws/(.*) ws://localhost:9353/$1/ws/$2 [P]
</Location>